Rarefied design, by its nature, cannot be constrained and thus science lacks methodology to reliably detect it using the "design inference" method proposed by ID.Simple really, if the design inference would lead to false positives (inferring design where there wasn't) it is useless per Dembski's own statements.
